import { DescripcionClimaPipe } from './descripcion-clima-pipe';

describe('DescripcionClimaPipe', () => {
  it('create an instance', () => {
    const pipe = new DescripcionClimaPipe();
    expect(pipe).toBeTruthy();
  });
});
